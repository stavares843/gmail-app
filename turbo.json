{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**", "build/**", "**/.next/**"],
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false
    }
  }
}
